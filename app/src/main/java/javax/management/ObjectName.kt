package javax.management

/**
 * Minimal stub. Only referenced as a return type
 * (PlatformManagedObject.getObjectName()) which the Kotlin compiler's
 * PerformanceManager never actually calls — this class only needs to
 * exist so bytecode verification succeeds when loading types that
 * reference it, not to do anything real.
 */
class ObjectName
